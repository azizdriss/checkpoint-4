import React from 'react';
import { Link } from 'react-router-dom';
const Wrapper = (props) => {
    return (
      <div>
      {props.children}
      </div>
    )
  }
  export default Wrapper